{
  "name": "parking-management-system",
  "version": "1.0.0",
  "description": "Parking Management System - Frontend and Backend",
  "private": true,
  "scripts": {
    "build": "cd parking-front && npm install && npm run build",
    "start:frontend": "cd parking-front && npm start",
    "start:backend": "cd parking-back && npm start",
    "install:frontend": "cd parking-front && npm install",
    "install:backend": "cd parking-back && npm install",
    "install:all": "npm run install:frontend && npm run install:backend"
  },
  "workspaces": [
    "parking-front",
    "parking-back"
  ]
}
